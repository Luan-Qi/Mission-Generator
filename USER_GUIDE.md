# Mission Generator — 用户操作手册

> **版本**: v1.2 | **适用平台**: Linux / Windows / macOS | **最后更新**: 2026-06-27

---

## 目录

1. [软件简介](#1-软件简介)
2. [安装与启动](#2-安装与启动)
3. [界面概览](#3-界面概览)
4. [第一步：打开点云文件](#4-第一步打开点云文件)
5. [3D 视图操作（鼠标/键盘）](#5-3d-视图操作鼠标键盘)
6. [添加航点 — 方式一：鼠标点选（推荐）](#6-添加航点--方式一鼠标点选推荐)
7. [添加航点 — 方式二：手动输入](#7-添加航点--方式二手动输入)
8. [设置起降点（可选）](#8-设置起降点可选)
9. [编辑与管理航点](#9-编辑与管理航点)
10. [渲染设置：调整显示效果](#10-渲染设置调整显示效果)
11. [保存与加载航点集](#11-保存与加载航点集)
12. [生成 ROS Launch 文件](#12-生成-ros-launch-文件)
13. [关闭文件](#13-关闭文件)
14. [快捷键速查表](#14-快捷键速查表)
15. [常见问题与故障排除](#15-常见问题与故障排除)

---

## 1. 软件简介

**Mission Generator** 是一款跨平台的桌面工具，用于在 **3D 点云地图** 上可视化选取无人机任务航点，并一键生成与 `mission_manger.cpp` 完全兼容的 **ROS1 launch 文件**。

### 核心功能

| 功能 | 说明 |
|------|------|
| **3D 点云可视化** | 加载 `.pcd` 格式点云文件，按高度（Z 轴）着色渲染 |
| **鼠标点选航点** | 在 3D 点云上**中键点击**直接拾取坐标，实时预览后添加航点 |
| **手动输入航点** | 精确输入 X/Y/Z 坐标添加航点 |
| **航点属性配置** | 每个航点可设置等待时间（默认/自定义/永久）、录像标记、备注标签 |
| **起降点可视化** | 设置 Home 位置，3D 视图中展示完整飞行路径及方向箭头 |
| **Launch 生成** | 一键生成 ROS1 XML launch 文件，所有 ROS 参数可配置 |
| **航点集管理** | JSON 格式保存/加载航点，方便任务复用 |

### 支持的 PCD 格式

- **ASCII** — 文本格式（最常见）
- **Binary** — 二进制格式
- **Binary Compressed** — LZF 压缩格式（需 `python-lzf` 包）

---

## 2. 安装与启动

### 方式一：可执行文件（推荐，无需安装 Python）

这是最简单的使用方式，适合不需要修改源码的用户。

1. 从 [Releases](../../releases) 页面下载对应平台的压缩包：

   | 平台 | 文件名 |
   |------|--------|
   | **Linux** | `MissionGenerator` |
   | **Windows** | `MissionGenerator.exe` |
   | **macOS** | `MissionGenerator.app` |

2. **Linux 用户**：
   ```bash
   # 赋予执行权限
   chmod +x MissionGenerator
   
   # 方式 A：直接运行
   ./MissionGenerator
   
   # 方式 B：安装到系统（可选，会在应用启动器中显示图标）
   ./install.sh
   ```

3. **Windows 用户**：双击 `MissionGenerator.exe` 即可运行。

4. **macOS 用户**：双击 `MissionGenerator.app`。

> **系统要求**：仅需 **OpenGL 2.1+** 显卡驱动，所有现代操作系统均已自带。无需安装 Python 或任何依赖包。

### 方式二：从源码运行

适合需要修改代码或二次开发的用户。

```bash
# 1. 克隆仓库
git clone <repo-url>
cd mission_generator

# 2. 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate   # Linux/macOS
# 或 venv\Scripts\activate  (Windows)

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动程序
python main.py
```

**依赖清单**：

| 包名 | 版本要求 | 说明 |
|------|---------|------|
| `PySide6` | >= 6.5 | Qt for Python GUI 框架 |
| `numpy` | >= 1.24 | 数值计算 |
| `vispy` | >= 0.14 | OpenGL 3D 渲染引擎 |
| `python-lzf` | 可选 | Binary Compressed PCD 支持 |
| `pyinstaller` | >= 6.0 | 打包工具（仅打包时需要） |

---

## 3. 界面概览

启动程序后，你会看到如下布局：

```
┌──────────────────────────────────────────────────────────────────┐
│  菜单栏: 文件  视图  帮助                      Mission Generator    │
├──────────────────────────────────────────────────────────────────┤
│  [📂 打开PCD] [💾 保存航点] [📥 加载航点] [🚀 生成Launch]            │
│  [🔄 重置视角] [⚙️ 渲染]    🖱 操作提示...                          │
├─────────────────────────┬────────────────────────────────────────┤
│                         │  ┌─ 起降点(可选) ─────────────────┐      │
│                         │  │ X:___ Y:___ Z:___             │     │
│      3D 点云视图         │  │ [设为起降点] [清除起降点]        │      │
│      (左侧，主区域)       │  └───────────────────────────────┘     │
│                         │                                        │
│    ◆ = 起降点 Home       │  ┌─ 手动添加航点 ───────────────────┐    │
│    ● = 航点 + 序号       │  │ X:___ Y:___ Z:___             │     │
│    ◇ = 预览标记          │  │ 等待:[使用默认 ▼] 等待时间       │     │
│    · = 点云原始点         │  │ ☐ 录像标记  标签:[______]       │    │
│    ─→ = 飞行路径+箭头     │  │ [添加航点]                     │     │
│                         │  └───────────────────────────────┘     │
│                         │                                        │
│                         │  ┌─ 航点列表 ─────────────────────┐     │
│                         │  │ # │ X  │ Y  │ Z │等待│录像│标签 │     │
│                         │  │ 1 │35.0│ 0.0│0.6│ 默认│ ☑ │    │    │
│                         │  │ 2 │52.0│-10.│0.8│ ∞  │ ☐ │    │     │
│                         │  └────────────────────────────────┘    │
│                         │  [↑上移] [↓下移] [删除选中] [清空全部]     │
│                         │  共 N 个航点                            │
├─────────────────────────┴────────────────────────────────────────┤
│  状态栏: PCD: room.pcd (1,024,000 点) | 航点数: 3                  │
└──────────────────────────────────────────────────────────────────┘
```

| 区域 | 说明 |
|------|------|
| **菜单栏** | 文件操作（打开/关闭/保存/加载/生成 Launch）、视图控制、帮助 |
| **工具栏** | 常用操作的快捷按钮 |
| **3D 视图** | 左侧主区域，显示点云、航点、路径等 3D 内容 |
| **右侧面板** | 起降点设置、手动添加航点、航点列表表格 |
| **状态栏** | 底部，显示当前 PCD 文件名、点数和航点数 |

---

## 4. 第一步：打开点云文件

### 操作步骤

1. 点击工具栏 **📂 打开PCD** 按钮，或使用快捷键 **Ctrl+O**，或通过菜单 **文件 → 打开 PCD 文件...**

2. 在弹出的文件选择对话框中，找到你的 `.pcd` 文件，选中后点击"打开"。

   > 💡 **提示**：支持的扩展名为 `.pcd` 和 `.PCD`。

3. 程序会读取并解析 PCD 文件：
   - 状态栏显示文件名和点数
   - 3D 视图自动加载点云，按 Z 轴高度着色（蓝色=低处 → 绿色=中间 → 红色=高处）
   - 视角自动调整为适合观察的位置

### 点数限制

程序限制单次加载 **最多 10,000,000（一千万）个点**。如果点云超过此限制，会弹出错误提示，建议使用 `pcl_voxel_grid` 等工具进行降采样后再打开。

### 关闭当前文件

如需关闭当前点云，使用 **Ctrl+W** 或菜单 **文件 → 关闭文件**。如果有未保存的航点，会弹出确认对话框。

> ⚠️ **重要**：关闭文件或打开新文件时，**当前所有航点将被清空**。请提前使用 **Ctrl+S** 保存航点集。

---

## 5. 3D 视图操作（鼠标/键盘）

### 鼠标操作

| 操作 | 效果 |
|------|------|
| **左键拖拽** | 旋转视角（绕中心点旋转） |
| **滚轮滚动** | 缩放（拉近/拉远） |
| **Shift + 左键拖拽** | 平移视角（上下左右移动） |
| **中键点击点云** | 🎯 **添加航点**（拾取点击位置的坐标） |
| **左键点击点云** | 拾取坐标填入右侧面板输入框（不直接添加） |

### 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| **R** | 重置视角 |
| **Ctrl+O** | 打开 PCD 文件 |
| **Ctrl+W** | 关闭当前文件 |
| **Ctrl+S** | 保存航点集 |
| **Ctrl+L** | 加载航点集 |
| **Ctrl+G** | 生成 Launch 文件 |
| **Ctrl+Q** | 退出程序 |

### 调整平移速度

Shift+左键平移的默认速度是 **20×**，可通过以下方式调整：

1. 点击 **⚙️ 渲染** 按钮
2. 在"航点/航线渲染"区域找到"**Shift+左移 速度**"滑块
3. 拖动调整，范围 **1× ~ 100×**（1 最慢，100 最快）
4. 松开滑块后自动应用

---

## 6. 添加航点 — 方式一：鼠标点选（推荐）

这是**最直观、最高效**的添加方式。

### 操作流程

**第 1 步**：在 3D 视图中找到目标位置（可旋转/缩放/平移视角辅助定位）。

**第 2 步**：**中键点击**点云上的目标位置。

> 🖱️ 鼠标中键 = 滚轮按下（不是滚动，是按下滚轮）

**第 3 步**：点击后：
- 一个 **黄色菱形 (◇)** 预览标记立即出现在拾取位置
- 弹出**悬浮**航点对话框（不阻挡 3D 视图，仍可旋转视角确认位置）

**第 4 步**：在悬浮对话框中确认/调整航点参数：

| 参数 | 说明 |
|------|------|
| **X / Y / Z** | 航点坐标，可手动微调 |
| **等待时间** | 下拉选择：使用默认 / 自定义(秒) / 永久等待(∞) |
| **录像标记** | 勾选后航点带 `*` 标记，触发该航点段的视频录制 |
| **标签** | 可选的备注文字 |

> 💡 **实时预览**：修改 X/Y/Z 坐标时，3D 视图中的黄色预览标记会**实时跟随移动**，方便你确认位置是否准确。

**第 5 步**：
- 点击 **确定** → 正式航点添加，预览标记变为红色球体 + 序号标签，3D 路径连线更新
- 点击 **取消** → 预览标记消失，不添加航点

### 注意事项

- 点选精度取决于点云密度 — 在稀疏区域可能选不到点（鼠标指针附近没有点云点），此时可尝试放大视图或换到密度更高的区域点击
- 如果多次点击同一位置都选不到点，建议使用**手动输入方式**添加

---

## 7. 添加航点 — 方式二：手动输入

适合已知精确坐标或点选困难的场景。

### 操作步骤

**第 1 步**：在右侧面板"**手动添加航点**"区域：

```
┌─ 手动添加航点 ───────────────────┐
│ X: [____] Y: [____] Z: [____]  │
│ 等待: [使用默认 ▼]  [____]       │
│ ☐ 录像标记 (*)  标签: [____]     │
│ [ 添加航点 ]                    │
└────────────────────────────────┘
```

**第 2 步**：输入 X、Y、Z 坐标值（单位：米）。

> 💡 **技巧**：你也可以先在 3D 视图中**左键点击**点云上的某个位置，坐标会自动填入 X/Y/Z 输入框，然后手动调整。

**第 3 步**：设置等待时间：

| 下拉选项 | 含义 | launch 中表现 |
|----------|------|--------------|
| **使用默认** | 使用 launch 文件中的全局 `wait_time` 参数 | 不写第 4 参数 |
| **自定义 (秒)** | 该航点专属等待时间 | `[x, y, z, 5.0]` |
| **永久等待 (∞)** | 无人机悬停直到外部指令 | `[x, y, z, 0]` |

**第 4 步**（可选）：
- 勾选 **☐ 录像标记 (\*)**，触发该航点段视频录制
- 在标签栏输入备注文字

**第 5 步**：点击 **添加航点** 按钮，航点出现在 3D 视图和航点列表中。

---

## 8. 设置起降点（可选）

起降点（Home Position）用于在 3D 视图中展示完整的飞行路径：**Home -> WP1 -> WP2 -> ... -> Home**。

> ⚠️ **重要**：起降点仅用于可视化，**不会写入 launch 文件的 waypoints 参数**。launch 文件中的第一个航点就是第一个任务目标点。

### 操作步骤

**第 1 步**：在右侧面板"**起降点(可选)**"区域输入 X、Y、Z 坐标。

**第 2 步**：点击 **设为起降点**（绿色按钮）。

**第 3 步**：3D 视图中：
- 出现 **绿色方块 (◆)** 表示起降点位置
- 出现 **飞行路径连线**：Home -> WP1 -> WP2 -> ... -> Home
- 路径上的 **锥形箭头** 指示运动方向

**清除起降点**：点击 **清除起降点** 按钮。

---

## 9. 编辑与管理航点

### 航点列表

右侧面板下方是航点列表表格：

```
# │  X   │  Y   │  Z   │ 等待 │ 录像 │ 标签
1 │35.000│ 0.000│ 0.600│ 默认 │  ☑   │ 入口
2 │52.000│-10.00│ 0.800│  ∞   │  ☐   │ 目标A
3 │52.000│ 0.000│ 1.000│ 5.0s │  ☑   │ 目标B
```

### 表格操作

| 操作 | 方法 |
|------|------|
| **编辑坐标** | 双击 X/Y/Z 单元格，输入新值，按 Enter 确认 |
| **编辑等待时间** | 双击"等待"单元格，输入数值（秒），或输入 `默认`/`∞`/`0` |
| **切换录像标记** | 直接点击录像列的 **☑ 复选框** |
| **编辑标签** | 双击"标签"单元格，输入文字 |
| **删除航点** | 选中行（可多选 Ctrl+点击），右键 → 删除选中，或点击下方 **删除选中** 按钮 |
| **调整顺序** | 选中单个航点，点击 **↑ 上移** / **↓ 下移** |
| **清空全部** | 点击 **清空全部** 按钮（有确认对话框） |

### 右键菜单

在表格上右键点击，弹出快捷菜单：
- 删除选中
- 上移 / 下移
- 清空全部

---

## 10. 渲染设置：调整显示效果

点击工具栏 **⚙️ 渲染** 按钮，打开渲染设置对话框：

### 点云渲染

| 设置 | 范围 | 说明 |
|------|------|------|
| **点大小** | 1 ~ 20 px | 点云中每个点的像素大小 |
| **透明度** | 5% ~ 100% | 点云整体透明度 |
| **颜色模式** | 5 种 | Z 高度(蓝→红) / 白色 / 青色 / 绿色 / 橙色 |

### 航点/航线渲染

| 设置 | 范围 | 说明 |
|------|------|------|
| **航点大小** | 4 ~ 50 px | 航点球体的直径 |
| **航线颜色** | 6 种预设 | 飞行路径连线的颜色 |
| **航线宽度** | 0.5 ~ 10 px | 飞行路径连线的粗细 |
| **Shift+左移 速度** | 1 ~ 100× | 平移视角的速度倍率（默认 20×） |
| **航点深度测试** | 开/关 | 关闭后航点始终可见（不被点云遮挡） |

> 💡 **推荐设置**：点大小 2~3px，透明度 80~100%，航点深度测试关闭（航点始终可见）。

---

## 11. 保存与加载航点集

### 保存航点集

1. 点击 **💾 保存航点** 或按 **Ctrl+S**
2. 选择保存位置和文件名（JSON 格式）
3. 航点集包含：所有航点的坐标、等待时间、录像标记、标签、起降点信息

### 加载航点集

1. 点击 **📥 加载航点** 或按 **Ctrl+L**
2. 选择之前保存的 `.json` 文件
3. 航点自动恢复到列表和 3D 视图中

> 💡 **使用场景**：为不同任务保存不同的航点集文件，下次需要执行相同任务时直接加载。

---

## 12. 生成 ROS Launch 文件

### 操作步骤

**第 1 步**：点击 **🚀 生成Launch** 或按 **Ctrl+G**。

> ⚠️ 如果当前没有航点，会提示是否生成空的 launch 文件。

**第 2 步**：在弹出的对话框中配置参数：

#### Tab 1 — ROS 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| **包名 (pkg)** | `uav_px4_ctrl` | ROS 包名 |
| **节点类型 (type)** | `mission_manger` | 可执行文件名 |
| **节点名 (name)** | `mission_manger` | ROS 节点名 |
| **odom_topic** | `/localization` | 里程计话题（优先级高于 pose_topic） |
| **pose_topic** | `/mavros/local_position/pose` | 位姿话题 |
| **goal_topic** | `/goal_pose` | 目标点发布话题 |
| **distance_threshold** | `0.5` m | 到达判定距离阈值 |
| **wait_time** | `5.0` s | 到达后默认等待时间 |
| **start_delay** | `3.0` s | 起飞后启动延迟 |
| **topic_timeout** | `2.0` s | 位置话题超时 |
| **mission_cycle** | ☑ 循环 | 是否循环执行任务 |

#### Tab 2 — 航点预览

- 显示所有航点的只读预览表格
- 显示生成的航点字符串格式（如 `[[35.0, 0.0, 0.6]*, [52.0, -10.0, 0.8]]`）

**第 3 步**：点击 **浏览...** 选择 launch 文件的保存路径。

**第 4 步**：点击 **生成 Launch 文件**。

### 生成的 Launch 文件格式

```xml
<launch>
    <!-- ================================================ -->
    <!--  任务管理器 Launch 文件                           -->
    <!--  生成工具: Mission Generator v1.2                 -->
    <!--  生成时间: 2026-06-27 10:30:00                    -->
    <!--  航点数量: 3                                      -->
    <!-- ================================================ -->

    <arg name="odom_topic" default="/localization"/>
    <arg name="pose_topic" default="/mavros/local_position/pose"/>
    <arg name="goal_topic" default="/goal_pose"/>
    <arg name="distance_threshold" default="0.5"/>
    <arg name="wait_time" default="5.0"/>
    <arg name="start_delay" default="3.0"/>
    <arg name="topic_timeout" default="2.0"/>
    <arg name="mission_cycle" default="true"/>
    <arg name="waypoints" default="[[35.0, 0.0, 0.6]* , [52.0, -10.0, 0.8] , [52.0, 0.0, 1.0]*]"/>

    <node pkg="uav_px4_ctrl" type="mission_manger" name="mission_manger" output="screen">
        <param name="odom_topic" value="$(arg odom_topic)"/>
        <param name="pose_topic" value="$(arg pose_topic)"/>
        <param name="goal_topic" value="$(arg goal_topic)"/>
        <param name="distance_threshold" value="$(arg distance_threshold)"/>
        <param name="wait_time" value="$(arg wait_time)"/>
        <param name="start_delay" value="$(arg start_delay)"/>
        <param name="topic_timeout" value="$(arg topic_timeout)"/>
        <param name="mission_cycle" value="$(arg mission_cycle)"/>
        <param name="waypoints" value="$(arg waypoints)"/>
    </node>
</launch>
```

### 在 ROS 中使用

```bash
roslaunch <生成的launch文件路径>
```

> 生成的 launch 文件与 `uav_px4_ctrl` 包中的 `mission_manger` 节点完全兼容，航点字符串格式严格匹配 C++ 正则解析规则。

---

## 13. 关闭文件

### 关闭当前点云

- 点击菜单 **文件 → 关闭文件** 或按 **Ctrl+W**
- 如果当前有航点，会弹出确认对话框
- 关闭后 3D 视图清空，回到初始状态

### 退出程序

- 点击菜单 **文件 → 退出** 或按 **Ctrl+Q**
- 或直接关闭窗口
- 如果有未保存的航点，会弹出保存提示

---

## 14. 快捷键速查表

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+O` | 打开 PCD 文件 |
| `Ctrl+W` | 关闭当前文件 |
| `Ctrl+S` | 保存航点集 (JSON) |
| `Ctrl+L` | 加载航点集 (JSON) |
| `Ctrl+G` | 生成 ROS Launch 文件 |
| `Ctrl+Q` | 退出程序 |
| `R` | 重置 3D 视角 |
| 鼠标左键拖拽 | 旋转视角 |
| 滚轮滚动 | 缩放 |
| `Shift` + 左键拖拽 | 平移视角 |
| 中键点击 (点云上) | 拾取坐标并添加航点 |
| 左键点击 (点云上) | 拾取坐标填入输入框 |

---

## 15. 常见问题与故障排除

### Q1: 程序启动后占用内存很大？

**A**: 正常。程序启动后占用约 **150~180 MB** 内存（主要是 Python 运行时和 Qt 框架）。加载点云后会根据点数增加，百万点约增加 50~100 MB。如果内存持续增长到 1GB 以上，请重启程序。

### Q2: 中键点击点云没有反应？

**A**: 可能原因：
1. **鼠标指针位置没有点云点** — 尝试放大视图或移动到点云密度更高的区域
2. **鼠标中键被其他程序占用** — 检查系统鼠标设置
3. **点云未加载** — 确认已通过 Ctrl+O 成功打开 PCD 文件

### Q3: 为什么航点在某些角度看不到？

**A**: 检查渲染设置中的"**航点深度测试**"开关。如果开启了深度测试，航点可能被点云遮挡。建议关闭此选项（默认关闭），使航点始终绘制在最上层。

### Q4: 生成的 launch 文件能在 ROS 中使用吗？

**A**: 可以。生成的 launch 文件格式与 `uav_px4_ctrl` 包的 `mission_manger` 节点完全兼容。航点字符串严格匹配 C++ 正则：

```
\[\s*([-0-9\.eE]+)\s*,\s*([-0-9\.eE]+)\s*,\s*([-0-9\.eE]+)(?:\s*,\s*([0-9\.eE]+))?\s*\]\s*(\*?)
```

### Q5: 打包后的程序有多大？

**A**: 约 **100~130 MB**（PyInstaller 单文件打包，包含 Python 运行时和所有依赖）。

### Q6: 支持哪些 PCD 格式？

**A**: 完整支持三种 PCD 数据模式：
- **ASCII** — 文本格式 ✓
- **Binary** — 二进制格式 ✓
- **Binary Compressed** — LZF 压缩格式 ✓（从源码运行时需 `pip install python-lzf`）

### Q7: 程序无法启动/闪退？

**A**: 
1. **Linux**: 确保有 OpenGL 驱动：`glxinfo | grep "OpenGL version"`
2. **Windows**: 确保安装了 VC++ Redistributable（Windows 10+ 已自带）
3. **从源码运行**: 从终端运行 `python main.py` 查看错误日志

### Q8: 航点列表中的"录像"列怎么操作？

**A**: "录像"列显示为 **复选框 (☑)**，直接点击即可切换开关状态。勾选表示该航点带有 `*` 录像标记，在 launch 文件中对应的航点格式为 `[x, y, z]*`。

### Q9: 3D 视图的坐标轴出现黑块？

**A**: 这是已知的 vispy 渲染问题，已在 v1.2.2 版本中修复。如果仍遇到此问题，请更新到最新版本。

### Q10: 如何获取帮助或报告问题？

**A**: 请在项目的 [Issues](../../issues) 页面提交问题报告，附上：
- 操作系统及版本
- 程序版本
- 错误截图（如有）
- 复现步骤
- 终端中的错误日志（如有）

---

## 附录：航点字符串格式参考

### 格式规则

```
[wpx, wpy, wpz, wait_time?] 后缀?
```

| 格式 | 含义 |
|------|------|
| `[x, y, z]` | 基础航点，使用全局默认等待时间 |
| `[x, y, z]*` | 带录像标记（`*` 后缀） |
| `[x, y, z, 5.0]` | 自定义等待 5.0 秒 |
| `[x, y, z, 0]` | 永久等待 (0 = ∞) |
| `[x, y, z, 5.0]*` | 自定义等待 5.0 秒 + 录像标记 |

### 全局格式

```
[[wp1, wp2, wp3, ...]]
```

---

> **文档版本**: v1.0 | **适用软件版本**: Mission Generator v1.2+
